public interface Pet {
    // so we can implement one or more interfaces
    // but when inheriting we can only inherit from only one super class (in Java)
    // in Java because in C++ we can do so (inherit from more than one class)
    void sneakingAround();
}
